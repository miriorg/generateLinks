# generateLinks.ToDo.md

> このファイルは追記のみで運用します。内容の削除は行いません。

---

## 依頼内容(現時点の理解)

- `prompt.md` の仕様に従い、**リストファイルを読み込んで、指定したリンク元ファイルのリンク(シンボリックリンク／ハードリンク)を一括作成するスクリプト**を作成する。
- 主なオプション:
  - `--target`（必須。位置引数のみでも target 指定とみなす）: リンク元ファイル
  - `--source`: リストファイルのパス（デフォルトはスクリプトのあるフォルダの `target.list`）
  - `--verbose`: 動作結果を一覧表示
  - `--check`: リストの各エントリが存在するか等の一覧を表示
  - `--overwrite` / `-o`: 同名ファイルがある場合に上書き（デフォルトはスキップ）
  - `--hard`: シンボリックリンクではなくハードリンクを作成
  - パラメータなし実行で usage 表示
- 出力は `prompt.md` 内の Result / Exists / Type / Path / Description（および check 時は Link to）列の表形式。

## 依頼日時

- 2026-09-01（`@prompt.md` を読み込んで実施、との指示）

## 現在依頼に対応している状況

- **実装完了（2026-09-01）**。`powershell` 5.1 / `pwsh` 7.6.5 の両方で
  `Test-generateLinks.ps1` が PASS=35 / FAIL=0 / SKIP=1（SKIP はこのセッションに
  シンボリックリンク作成権限が無いため。開発者モード有効化または管理者実行で実行可）。
- 成果物: `generateLinks.ps1` / `generateLinks.format.ps1xml` / `target.list` /
  `Test-generateLinks.ps1` / `README.md`。進行記録は `Build-ToDo.md`。
- （履歴）最初のヒアリング中はコーディング未着手。`prompt.md` 末尾の指示により
  不明点が解消するまで着手しなかった。

## 決定事項

### 2026-09-01 ヒアリング回答を反映

- **言語/構成**: PowerShell `generateLinks.ps1` 単体。Windows PowerShell 5.1 を主対象、PowerShell 7+ でも動作可を目指す。ラッパー(.cmd)は作らない。
- **リストファイル**:
  - 1 行 1 パス。`#` 始まりの行と空白のみの行は無視。
  - リンク先の相対パスは「リストファイルのある場所」を基準に解決。
  - `%VAR%` / `$env:VAR` / 先頭 `~` を展開する。
  - UTF-8（BOM 有無どちらも読み取り可）。
- **target(リンク元)**:
  - 相対パスはカレントディレクトリ基準。
  - 存在しなければエラー表示して即終了（終了コード 2）。
  - **フォルダも対象可**。Symbolic Link 時はディレクトリシンボリックリンクを作成。`--hard` 指定時はフォルダ不可のため当該行 fail で継続。
- **source(リストファイル)オプション**:
  - デフォルトは常にスクリプトのあるフォルダの `target.list`（`--hard` でも同じ）。
  - `--source` の相対パスはカレントディレクトリ基準。
- **overwrite**:
  - デフォルト（スキップ）: 既存が実体でもリンクでも一切変更しない。
  - `--overwrite` / `-o`: 上書き対象は **リンク（Symbolic/Hard）のみ**。実体ファイル・フォルダは保護して skip。
  - `--overwrite` かつ既存が別種リンク: 既存リンクを削除して要求どおりの種類で作り直す。
- **親フォルダ不在**: 作成しない。`Result=skip / Exists=false / Type=- / Description=Folder not exists.`。
- **出力**:
  - 通常実行（`--verbose`/`--check` なし）は原則無出力。致命的エラーのみ表示。
  - メッセージは英語（`created.` 等）。
  - `--check` は `--verbose` より優先。`--check` 時はリンク作成を行わず一覧のみ出力。
  - 結果は `[PSCustomObject]` を出力し、既定表示は `Format-Table` に委ねる（パイプ利用可）。列順・見出しは prompt.md に準拠。
  - `Source:` 行は target を解決した絶対パス。
- **ハードリンク不可条件**（別ボリューム・フォルダ等）: `Result=fail` ＋理由を Description に出して継続。
- **`--check` の Type=HardLink "Link to" 列**: 同一実体を指す別パスを 1 つ表示（無ければ `-`）。
- **権限不足（Symbolic Link 作成不可）**: 自動昇格せず、開発者モード有効化／管理者実行を案内するエラーを表示して終了。
- **ログ**: CLI は `--verbose` の ON/OFF のみ（詳細は Q2 で最終確認）。
- **終了コード**: 全件 success/skip=0、fail が 1 件でもあれば 1、引数エラー/target 不在=2。
- **オプション**: `-t`/`--target`, `-s`/`--source`, `-v`/`--verbose`, `-o`/`--overwrite`, `--hard`, `--check`, `-h`/`--help`。位置引数は最大 1 個（= target）。
- **追加オプション（採用）**:
  - `--dry-run`: 実際の作成・上書きを行わず、操作予定のみ一覧表示（提案1）。
  - `--check` 拡張: 既存 Symbolic Link の実リンク先が target と不一致なら Description に注記（提案2）。
- **テスト（採用）**: 一時フォルダ内で完結する分岐テストを同梱（新規作成 / skip / overwrite / 親フォルダ無し / hard / check / dry-run）。
  サンプル `target.list` は既知の生成 AI 指示ファイルの慣用パス（`.claude/CLAUDE.md`, `.gemini/GEMINI.md`, `.github/copilot-instructions.md`, `.cursor/rules/*` 等の**パス名のみ**）を使用。ファイル内容(著作物)は取得せず、テスト実体はダミー生成。

## 制限事項

- 対象 OS は **Windows のみ**（NTFS 前提。Symbolic/Hard/Junction の概念に依存）。
- Symbolic Link 作成には管理者権限または「開発者モード」が必要。未充足時は作成不可（スクリプトは案内のみ、昇格しない）。
- Hard Link は同一ボリューム内・ファイルのみ（フォルダ不可）。
- リストファイルの 1 行 = 1 パス。引用符・エスケープを含む特殊パスの網羅対応は範囲外。
- `--overwrite` でも実体ファイル／フォルダは削除しない（リンクのみ置換）。
- UNC パスや特殊な再解析ポイントは基本動作のみ保証（網羅的検証は範囲外）。
- サンプルとして外部の生成 AI 指示ファイルの**本文は収集しない**（慣用パス名のみ利用）。

---

## ヒアリング項目

> 各項目、`→ 回答:` の行に選択肢の記号や自由記述を追記してください。推奨案を先頭に置いています。

### 2026-09-01 初回ヒアリング

#### A. 実装言語・実行環境

- **A1. 実装言語**
  - (a) PowerShell スクリプト `generateLinks.ps1`（推奨。CLAUDE.md の既定に合致）
  - (b) その他（言語を指定してください）
  - → 回答:(a)

- **A2. 対応 PowerShell バージョン**
  - (a) Windows PowerShell 5.1 と PowerShell 7+ の両対応（推奨）
  - (b) PowerShell 7+ のみ
  - (c) Windows PowerShell 5.1 のみ
  - → 回答:(a) 必要があれば7+でも可

- **A3. 呼び出し用ラッパー**
  - (a) `generateLinks.ps1` のみ（推奨）
  - (b) `generateLinks.cmd`（または `.bat`）ラッパーも用意し、`generateLinks CLAUDE.md` の形で起動できるようにする
  - → 回答:(a)

#### B. リストファイル(target.list / 任意のリスト)の仕様

- **B1. 記述形式**
  - (a) 1 行に 1 パス（リンク先＝作成するリンクのパス）（推奨）
  - (b) その他（区切り／複数列など指定してください）
  - → 回答:(a)

- **B2. リンク先パスの基準**
  - (a) 絶対パスと相対パスの両対応。相対パスは「リストファイルのある場所」を基準に解決（推奨）
  - (b) 相対パスは「カレントディレクトリ」基準
  - (c) 相対パスは「スクリプトのある場所」基準
  - (d) 絶対パスのみ許可（相対はエラー）
  - → 回答:(a)

- **B3. コメント行・空行**
  - (a) `#` で始まる行と空行（空白のみ含む）は無視する（推奨）
  - (b) 無視しない（全行をパスとして扱う）
  - → 回答:(a)

- **B4. 環境変数・チルダ展開**
  - (a) `%USERPROFILE%` や `$env:...`、先頭 `~` を展開する（推奨）
  - (b) 展開しない（記載どおりのリテラル）
  - → 回答:(a)

- **B5. リストファイルの文字コード**
  - (a) UTF-8（BOM 有無どちらも読める）を前提（推奨）
  - (b) その他指定
  - → 回答:(a)

#### C. target(リンク元ファイル)の仕様

- **C1. target 相対パスの基準**
  - (a) カレントディレクトリ基準（推奨。使用例「カレントディレクトリの CLAUDE.md」に合致）
  - (b) スクリプトのある場所基準
  - → 回答:(a)

- **C2. target が存在しない場合**
  - (a) エラーメッセージを表示して即終了（何も作成しない）（推奨）
  - (b) 警告のみ表示して継続（全件 skip 扱い）
  - → 回答:(a)

- **C3. target がフォルダの場合**
  - (a) 許可しない（ファイルのみ対象。フォルダはエラー）（推奨）
  - (b) 許可する（シンボリックリンク時はディレクトリシンボリックリンクを作成。ハードリンクはフォルダ不可なのでエラー）
  - → 回答:(b)

#### D. source(リストファイル)オプションの仕様

- **D1. デフォルトのリストファイル**
  - (a) 常に「スクリプトのあるフォルダの `target.list`」（推奨。prompt.md 記載どおり）
  - (b) `--hard` 指定時はデフォルトを `hardlink.list` に変える
  - → 回答:(a)

- **D2. `--source` 相対パスの基準**
  - (a) カレントディレクトリ基準（推奨。使用例 `--source .\hardlink.list`）
  - (b) スクリプトのある場所基準
  - → 回答:(a)

#### E. overwrite(上書き)の挙動 ※データ保護に関わる重要項目

- **E1. デフォルト(スキップ)時**
  - (a) 既存が実体でもリンクでも一切変更しない（Type と Description を出力して skip）（推奨）
  - → 回答:(a)

- **E2. `--overwrite` 指定時に「既存が実体ファイル／フォルダ」の場合**
  - (a) 実体は保護し skip する（上書きするのはリンク（Symbolic/Hard）のみ）（推奨・安全）
  - (b) 実体ファイルも削除して上書きする（危険。フォルダは常に保護）
  - (c) 実体ファイルも削除して上書きし、削除前に `.bak` を作る
  - → 回答:(a)

- **E3. `--overwrite` 指定時に「既存が別種のリンク」の場合**（例: `--hard` 指定だが既存は Symbolic Link）
  - (a) 既存リンクを削除して要求どおりの種類で作り直す（推奨）
  - (b) skip する
  - → 回答:(a)

#### F. リンク先の親フォルダが存在しない場合

- **F1. 挙動**
  - (a) 作成しない。`Result=skip / Exists=false / Type=- / Description=Folder not exists.` を出力（推奨。prompt.md の例に合致）
  - (b) `--mkdir` などのオプション時のみ親フォルダを再帰作成する
  - → 回答:(a)

#### G. 出力仕様

- **G1. `--verbose` 未指定・`--check` 未指定時の通常実行の出力**
  - (a) 原則無出力。ただしエラー（target 不在・権限不足など致命的なもの）は表示（推奨）
  - (b) 完全に無出力
  - (c) 1 行サマリ（success/skip/fail 件数）だけ表示
  - → 回答:(a)

- **G2. 出力メッセージの言語**
  - (a) prompt.md の例のとおり英語（`created.` / `File already exists.` 等）（推奨）
  - (b) 日本語
  - → 回答:(a)

- **G3. `--check` と `--verbose` の併用**
  - (a) `--check` が優先（`--check` 指定時は存在確認一覧のみ出力し、リンク作成は行わない）（推奨）
  - (b) 併用でリンク作成＋詳細表示も行う
  - → 回答:(a)

- **G4. 表の整形**
  - (a) prompt.md の例に近い等幅の整形テキスト（列見出し＋区切り線）。パイプライン利用は想定しない（推奨）
  - (b) PowerShell オブジェクトを出力し `Format-Table` に委ねる（他コマンドへパイプ可能にする）
  - → 回答:(b)

#### H. ハードリンク特有の扱い

- **H1. ハードリンク不可条件**（別ボリューム、対象がフォルダ 等）
  - (a) `Result=fail`（または skip）＋理由を Description に出して継続（推奨）
  - (b) 即時エラー終了
  - → 回答:(a)

- **H2. `--check` の "Link to" 列でのハードリンク表示**（ハードリンクは厳密な「リンク先」概念を持たないため）
  - (a) 同一実体を指す別パスを 1 つ検出して表示（見つからなければ `-`）（推奨）
  - (b) 常に `-`
  - (c) 判定に用いた target のパスを表示
  - → 回答:(a)かつ、リンク数を表示できるか？

#### I. 権限(シンボリックリンク作成)

- **I1. 管理者権限／開発者モードが無い環境**
  - (a) 自動昇格はせず、分かりやすいエラーメッセージ（開発者モード有効化 or 管理者実行を案内）を出して終了（推奨）
  - (b) 自動で管理者昇格（UAC）を試みる
  - → 回答:(a)

#### J. 動作ログ(CLAUDE.md「動作ログの表示について」準拠)

- **J1. ログレベルの構成と `--verbose` の統合**
  - (a) スクリプト冒頭に `$LogLevel`(`Debug` / `Detail` / `Simple`) フラグを持たせる。
        `--verbose` は `Detail` 相当、`--debug` は `Debug`、既定は `Simple`。
        開発完了報告後に既定を `Simple` へ切替（うるさくない表示に）（推奨）
  - (b) `--verbose` の ON/OFF のみで、追加のログレベルは設けない
  - → 回答:(b)

- **J2. ログの出力先**
  - (a) 画面のみ（推奨）
  - (b) 画面＋ログファイル（出力先パスを指定してください）
  - → 回答:(a)

#### K. 終了コード

- **K1. 規約**
  - (a) 全件成功/skip=0、1 件でも fail があれば 1、引数エラー/target 不在=2（推奨）
  - (b) 常に 0
  - (c) その他指定
  - → 回答:(a)

#### L. オプションのショート形式・エイリアス

- **L1. 割り当て確認**
  - 案（推奨）: `-t`/`--target`, `-s`/`--source`, `-v`/`--verbose`, `-o`/`--overwrite`,
    `--hard`（ショートなし）, `--check`（ショートなし）, `-h`/`--help`（`-h` は help に割当。hard にはショートを付けない）
  - → 回答:推奨案を採用

#### M. 位置引数

- **M1. 位置引数の扱い**
  - (a) 位置引数は最大 1 個（= target）。2 個以上渡されたら引数エラー（推奨）
  - (b) その他指定
  - → 回答:(a)

#### N. 出力の "Source:" 行

- **N1. 表示内容**
  - (a) target を解決した絶対パスを表示（推奨。例 `Source: C:\master\agent.md`）
  - → 回答:(a)

---

## 改善提案(実施可否のヒアリング)

### 提案1: `--dry-run`(実行せず予定のみ表示)オプションの追加

- **背景**: リンクの一括作成は影響範囲が広く、特に `--overwrite` 併用時は誤操作のリスクがある。
- **内容**: `--dry-run` 指定時は実際のリンク作成・上書きを行わず、「何をするか（success/skip/overwrite 予定）」の一覧だけを表示する。`--check` が「存在確認」なのに対し、`--dry-run` は「今回の実行で行われる操作の予告」。
- **メリット**: 事故防止、CI/バッチ前の確認が容易。
- **デメリット**: オプションと分岐が増える（実装・テスト工数 微増）。
- **必要リソース**: 実装 30〜60 分程度、テストケース追加。
- **提案日時**: 2026-09-01
- → 実施可否:可

### 提案2: リンク元との整合チェック（`--check` 拡張）

- **背景**: 既存のシンボリックリンクが、今回の target とは別のファイルを指している場合がある。
- **内容**: `--check` 時、既存 Symbolic Link の実際のリンク先が今回の target と一致するかを判定し、不一致なら印（例: Description に `points to another file`）を付ける。
- **メリット**: 古いリンクの検出が可能。
- **デメリット**: 出力列 or 注記が増える。
- **必要リソース**: 実装 20〜40 分。
- **提案日時**: 2026-09-01
- → 実施可否:可

### 提案3: テスト用サンプル(`target.list` サンプルと簡易テストスクリプト)の同梱

- **背景**: 手動確認は手間がかかり、リンク操作は環境を汚しやすい。
- **内容**: 一時フォルダ内で完結する Pester もしくは自作の簡易テスト（各分岐: 新規作成 / skip / overwrite / 親フォルダ無し / hard / check）を用意する。
- **メリット**: 回帰確認が容易、動作ログ要件の検証にもなる。
- **デメリット**: 追加ファイルとメンテ対象が増える。
- **必要リソース**: 実装 1〜2 時間。
- **提案日時**: 2026-09-01
- → 実施可否:可、一般的に公開情報で取得できる生成AIのでCLAUD.md/gemini.mdなどの実例を集めてサンプルとしたい

---

## 2026-09-01 追加確認(回答反映後の残論点)

- **Q1. H2 補足「ハードリンク数を表示できるか？」**
  - 可能です。`fsutil hardlink list <path>`（管理者権限不要）で同一実体を指す全パスを列挙でき、その件数＝ハードリンク数として表示できます（実体ファイルは 1）。
  - どこに出すか:
    - (a) `--check` の出力にのみ `Links`（整数）列を追加する（推奨）
    - (b) `--check` と `--verbose`（作成結果）の両方に `Links` 列を追加
    - (c) 追加しない（H2(a) の "Link to" 表示のみ）
  - → 回答:(a)

- **Q2. J1(ログ) と CLAUDE.md「動作ログの表示について」要件の整合**
  - ご回答は (b)「`--verbose` の ON/OFF のみ」でしたが、CLAUDE.md には「デバッグ／詳細／簡易の切替を用意」という要件があります。最小構成で両立させる案:
    - (a) CLI は `--verbose` のみ。加えてスクリプト冒頭に `$Script:LogLevel`（`Quiet` / `Normal` / `Debug`）定数を置く。既定 `Normal`、`--verbose` は Detail 相当の詳細表を出力、`Debug` は開発時のみ手動で有効化（完成報告後は既定を `Quiet` 寄りに切替）。CLI を増やさず CLAUDE.md 要件も満たす（推奨）
    - (b) 回答どおり厳密に `--verbose` のみとし、CLAUDE.md の動作ログ要件は本プロジェクトでは適用しない（この場で明示的に免除）
  - → 回答:(a)

---

## 2026-09-01 追加確認の確定

- **Q1 = (a)**: `--check` の出力にのみ `Links` 列（整数。実体=1、ハードリンクは `fsutil hardlink list` の件数）を追加する。
- **Q2 = (a)**: CLI オプションは `--verbose` の ON/OFF のみ。加えてスクリプト冒頭に `$Script:LogLevel`（`Quiet` / `Normal` / `Debug`）定数を持たせる。
  - 既定 `Normal`（通常実行は原則無出力、致命的エラーのみ）
  - `--verbose` 指定で Detail 相当（結果一覧を表示）
  - `Debug` は開発時に手動で有効化（関数名・主要フラグ・変数値を出力）。完成報告後は既定を静音側へ切替。

---

## 2026-09-01 仕様追加（実装完了後）

- **依頼**: `--source` 未指定時、既定の場所（スクリプトのフォルダ）に `target.list` が無ければ、
  カレントディレクトリの `target.list` も探す。
- **確定挙動**:
  1. `--source` 指定時: その値（カレント基準）を使用。見つからなければ `exit 2`。
  2. `--source` 未指定時:
     1. `<スクリプトのフォルダ>\target.list`
     2. 無ければ `<カレントディレクトリ>\target.list`
     3. どちらにも無ければ、探索した両パスを表示して `exit 2`。
- **反映先**: `generateLinks.ps1` の `Resolve-SourcePath` / `Show-Usage`、`prompt.md`、`README.md`、
  `Test-generateLinks.ps1`（`Invoke-SourceLookupScenario` を追加）。
- **テスト**: `powershell` 5.1 / `pwsh` 7.6.5 で PASS=39 / FAIL=0 / SKIP=1。

---

## 次のステップ

1. 上記ヒアリング項目への回答を受領
2. 回答内容を「決定事項」「制限事項」に反映（このファイルへ追記）
3. Plan モードで実装計画を提示 → 承認後 `Build-ToDo.md` を作成
4. `Build-ToDo.md` 承認後に実装着手
